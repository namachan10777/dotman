{
	"workdir": "/tmp",
	"install": [
		[ "rm -rf /tmp/__scripts_hooks/ghq" ],
		[ "mkdir -p /tmp/__scripts_hooks/ghq" ],
		[ "git clone https://github.com/motemen/ghq /tmp/__scripts_hooks/ghq" ],
		[ "make install -C /tmp/__scripts_hooks/ghq" ]
	],
	"check": [
		[ "test -e $GOPATH/bin/ghq" ]
	]
}
