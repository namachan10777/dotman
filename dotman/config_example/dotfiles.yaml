---
taskgroups:
  normalfile_common:
  - type: cp
    src: "alacritty/"
    dest: "$XDG_CONFIG_HOME/alacritty/"

scenarios:
- name: "sakanainu"
  match:
  - hostname: "^sakanainu$"
  tasks:
  - normalfile_common
